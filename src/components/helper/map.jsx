import {
    View, 

} from 'react-native';

export default function Map(props){
    return (
        <View style={props.mapStyle}>
            {/* This should be the component to show the map  */}
        </View>
    )
}